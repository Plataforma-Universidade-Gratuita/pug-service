PROJECT CONTEXT DUMP
Project Name: requests
Root Path: /c/Users/e-mateusf/Documents/college/PUG portfolio/pug-service/requests
================================================================================


================================================================================
FILE: academic/course/Create Course.bru
================================================================================
meta {
  name: Create Course
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/academic/courses
  body: json
  auth: none
}

headers {
  Accept-Language: pt-BR
  Content-Type: application/json
}

body:json {
  {
    "name": "Engenharia de Software",
    "schoolId": "{{schoolId}}"
  }
}

tests {
  test("Should create a course successfully", function() {
    expect(res.getStatus()).to.equal(201);
    expect(res.getBody().success).to.be.true;
    expect(res.getBody().data).to.have.property("id");

    // Save the created ID to the environment for student enrollment
    bru.setEnvVar("courseId", res.getBody().data.id);
  });
}

================================================================================
FILE: academic/course/Delete Course.bru
================================================================================
meta {
  name: Delete Course
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/academic/courses/{{courseId}}
  body: none
  auth: none
}

headers {
  Accept-Language: pt-BR
}

tests {
  test("Should delete the course successfully", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().success).to.be.true;
  });
}

================================================================================
FILE: academic/course/Get Course.bru
================================================================================
meta {
  name: Get Course
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/academic/courses/{{courseId}}
  body: none
  auth: none
}

headers {
  Accept-Language: pt-BR
}

tests {
  test("Should retrieve the course successfully", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().success).to.be.true;
    expect(res.getBody().data.id).to.equal(bru.getEnvVar("courseId"));
  });
}


================================================================================
FILE: academic/course/List Course.bru
================================================================================
meta {
  name: List or Search Courses
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/academic/courses
  body: none
  auth: none
}

params:query {
  ~q: Engenharia
  ~schoolId: {{schoolId}}
}

headers {
  Accept-Language: pt-BR
}

tests {
  test("Should list courses successfully", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().success).to.be.true;
    expect(Array.isArray(res.getBody().data)).to.be.true;
  });
}


================================================================================
FILE: academic/course/Update Course.bru
================================================================================
meta {
  name: Update Course
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/academic/courses/{{courseId}}
  body: json
  auth: none
}

headers {
  Accept-Language: pt-BR
  Content-Type: application/json
}

body:json {
  {
    "name": "Engenharia de Computação",
    "schoolId": "{{schoolId}}"
  }
}

tests {
  test("Should update the course successfully", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().success).to.be.true;
  });
}

================================================================================
FILE: academic/school/Create School.bru
================================================================================
meta {
  name: Create School
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/academic/schools
  body: json
  auth: none
}

headers {
  Accept-Language: pt-BR
  Content-Type: application/json
}

body:json {
  {
    "name": "Centro de Ciências Tecnológicas - CCT"
  }
}

tests {
  test("Should create a school successfully", function() {
    expect(res.getStatus()).to.equal(201);
    expect(res.getBody().success).to.be.true;
    expect(res.getBody().data).to.have.property("id");

    // Save the created ID to the environment for subsequent requests
    bru.setEnvVar("schoolId", res.getBody().data.id);
  });
}

================================================================================
FILE: academic/school/Delete School.bru
================================================================================
meta {
  name: Delete School
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/academic/schools/{{schoolId}}
  body: none
  auth: none
}

headers {
  Accept-Language: pt-BR
}

tests {
  test("Should delete the school successfully", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().success).to.be.true;
  });
}

================================================================================
FILE: academic/school/Get School.bru
================================================================================
meta {
  name: Get School
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/academic/schools/{{schoolId}}
  body: none
  auth: none
}

params:query {
  : 019cf2df-4a0f-70ca-bc5d-c240946456e3
}

headers {
  Accept-Language: pt-BR
}

tests {
  test("Should retrieve the school successfully", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().success).to.be.true;
    expect(res.getBody().data.id).to.equal(bru.getEnvVar("schoolId"));
  });
}


================================================================================
FILE: academic/school/List School.bru
================================================================================
meta {
  name: List or Search Schools
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/academic/schools
  body: none
  auth: none
}

query {
  ~q: Ciências
}

headers {
  Accept-Language: pt-BR
}

tests {
  test("Should list schools successfully", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().success).to.be.true;
    expect(Array.isArray(res.getBody().data)).to.be.true;
  });
}

================================================================================
FILE: academic/school/Update School.bru
================================================================================
meta {
  name: Update School
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/academic/schools/{{schoolId}}
  body: json
  auth: none
}

headers {
  Accept-Language: pt-BR
  Content-Type: application/json
}

body:json {
  {
    "name": "Centro de Ciências Tecnológicas Atualizado"
  }
}

tests {
  test("Should update the school successfully", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().success).to.be.true;
  });
}

================================================================================
FILE: academic/student/Create Student Bulk.bru
================================================================================
meta {
  name: Create Students Bulk
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/academic/students/bulk
  body: json
  auth: none
}

headers {
  Accept-Language: pt-BR
  Content-Type: application/json
}

body:json {
  [
    {
      "cpf": "52998224059",
      "name": "Maria Oliveira",
      "email": "maria.oliveira@aluno.com",
      "password": "SecurePassword123!",
      "academicRegistration": "20261002",
      "campus": "JOINVILLE",
      "courseId": "{{courseId}}",
      "requiredHours": 150.00,
      "startDate": "2026-03-15",
      "dueDate": "2028-12-15"
    },
    {
      "cpf": "15858285058",
      "name": "Carlos Souza",
      "email": "carlos.souza@aluno.com",
      "password": "SecurePassword123!",
      "academicRegistration": "20261003",
      "campus": "JARAGUA_DO_SUL",
      "courseId": "{{courseId}}",
      "requiredHours": 150.00,
      "startDate": "2026-03-15",
      "dueDate": "2028-12-15"
    }
  ]
}

tests {
  test("Should bulk create students successfully", function() {
    expect(res.getStatus()).to.equal(201);
    expect(res.getBody().success).to.be.true;
    expect(Array.isArray(res.getBody().data)).to.be.true;
  });
}


================================================================================
FILE: academic/student/Create Student.bru
================================================================================
meta {
  name: Create Student
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/academic/students
  body: json
  auth: none
}

headers {
  Accept-Language: pt-BR
  Content-Type: application/json
}

body:json {
  {
    "cpf": "73184295079",
    "name": "João da Silva",
    "email": "joao.silva@aluno.com",
    "password": "SecurePassword123!",
    "academicRegistration": "20261001",
    "campus": "JOINVILLE",
    "courseId": "{{courseId}}",
    "requiredHours": 120.00,
    "startDate": "2026-03-15",
    "dueDate": "2028-12-15"
  }
}

tests {
  test("Should create a student successfully", function() {
    expect(res.getStatus()).to.equal(201);
    expect(res.getBody().success).to.be.true;
    // The account ID is used as the student's primary identifier
    bru.setEnvVar("studentAccountId", res.getBody().data.account.id);
  });
}


================================================================================
FILE: academic/student/Delete Student.bru
================================================================================
meta {
  name: Delete Student
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/academic/students/{{studentAccountId}}
  body: none
  auth: none
}

headers {
  Accept-Language: pt-BR
}

tests {
  test("Should delete the student successfully", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().success).to.be.true;
  });
}

================================================================================
FILE: academic/student/Get Student By ID.bru
================================================================================
meta {
  name: Get Student By ID
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/academic/students/{{studentAccountId}}
  body: none
  auth: none
}

headers {
  Accept-Language: pt-BR
}

tests {
  test("Should retrieve the student successfully", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().success).to.be.true;
  });
}

================================================================================
FILE: academic/student/Get Student By Various Filters.bru
================================================================================
meta {
  name: Get Student By Various Filters
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/academic/students/by-registration/20261001
  body: none
  auth: none
}

headers {
  Accept-Language: pt-BR
}

docs {
  Toggle the URL to test different read operations:
  - {{baseUrl}}/academic/students/by-cpf/12345678900
  - {{baseUrl}}/academic/students/by-email/joao.silva@aluno.com
  - {{baseUrl}}/academic/students/by-registration/20261001
}

tests {
  test("Should retrieve the student successfully", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().success).to.be.true;
  });
}

================================================================================
FILE: academic/student/List Students.bru
================================================================================
meta {
  name: List or Search Students
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/academic/students
  body: none
  auth: none
}

query {
  ~q: João
  ~courseId: {{courseId}}
}

headers {
  Accept-Language: pt-BR
}

tests {
  test("Should list students successfully", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().success).to.be.true;
    expect(Array.isArray(res.getBody().data)).to.be.true;
  });
}

================================================================================
FILE: academic/student/Update Student.bru
================================================================================
meta {
  name: Update Student
  type: http
  seq: 6
}

put {
  url: {{baseUrl}}/academic/students/{{studentAccountId}}
  body: json
  auth: none
}

headers {
  Accept-Language: pt-BR
  Content-Type: application/json
}

body:json {
  {
    "name": "João da Silva Atualizado",
    "campus": "JARAGUA_DO_SUL",
    "requiredHours": 180.00
  }
}

tests {
  test("Should update the student successfully", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().success).to.be.true;
  });
}

================================================================================
FILE: bruno.json
================================================================================
{
  "version": "1",
  "name": "PUG Service API",
  "type": "collection",
  "ignore": [
    "node_modules",
    ".git"
  ]
}

================================================================================
FILE: environments/Local.bru
================================================================================
vars {
  baseUrl: http://localhost:8080
}

================================================================================
FILE: geo/city/Get City by IBGE.bru
================================================================================
meta {
  name: Get City by IBGE
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/geo/cities/by-ibge/4209106
  body: none
  auth: none
}

headers {
  Accept-Language: pt-BR
}

tests {
  test("Should retrieve the city by IBGE successfully", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().success).to.be.true;
    expect(res.getBody().data.ibgeCode).to.equal("4209106");
    expect(res.getBody().data.name).to.equal("Joinville");
  });
}

================================================================================
FILE: geo/city/Get City by ID.bru
================================================================================
meta {
  name: Get City by ID
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/geo/cities/{{cityId}}
  body: none
  auth: none
}

headers {
  Accept-Language: pt-BR
}

tests {
  test("Should retrieve the city successfully", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().success).to.be.true;
    expect(res.getBody().data.id).to.equal(bru.getEnvVar("cityId"));
  });
}

================================================================================
FILE: geo/city/List or Search Cities.bru
================================================================================
meta {
  name: List or Search Cities
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/geo/cities
  body: none
  auth: none
}

query {
  ~q: Joinville
}

headers {
  Accept-Language: pt-BR
}

tests {
  test("Should list cities successfully", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().success).to.be.true;
    expect(Array.isArray(res.getBody().data)).to.be.true;

    // Auto-capture the first city ID to use in the Get By ID request
    if (res.getBody().data.length > 0) {
      bru.setEnvVar("cityId", res.getBody().data[0].id);
    }
  });
}

================================================================================
FILE: identity/account/Get Account By ID.bru
================================================================================
meta {
  name: Get Account By ID
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/identity/accounts/019cf2fd-837e-7b34-b940-a8ab015fcea0
  body: none
  auth: none
}

headers {
  Accept-Language: pt-BR
}

tests {
  test("Should retrieve the account", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().success).to.be.true;
  });
}


================================================================================
FILE: identity/account/Get Accounts By Filters.bru
================================================================================
meta {
  name: Get Accounts By Filters
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/identity/accounts/by-email/admin.master@pug.com
  body: none
  auth: none
}

headers {
  Accept-Language: pt-BR
}

docs {
  Alterne a URL para testar os filtros de leitura puros da Account:
  - {{baseUrl}}/identity/accounts/by-email/admin.master@pug.com
  - {{baseUrl}}/identity/accounts/by-cpf/73184295079
}

tests {
  test("Should retrieve account by filter", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().success).to.be.true;
  });
}

================================================================================
FILE: identity/account/List or Search Accounts.bru
================================================================================
meta {
  name: List or Search Accounts
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/identity/accounts
  body: none
  auth: none
}

params:query {
  ~q: Administrador
}

headers {
  Accept-Language: pt-BR
}

tests {
  test("Should list accounts", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().success).to.be.true;
  });
}


================================================================================
FILE: identity/admin/Create Admin.bru
================================================================================
meta {
  name: Create Admin
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/identity/admins
  body: json
  auth: none
}

headers {
  Accept-Language: pt-BR
  Content-Type: application/json
}

body:json {
  {
    "cpfString": "73184295079",
    "name": "Administrador do Sistema",
    "emailString": "admin.master@pug.com",
    "password": "SuperSecretPassword123!",
    "campus": "JARAGUA_DO_SUL"
  }
}

tests {
  test("Should create an admin successfully", function() {
    expect(res.getStatus()).to.equal(201);
    expect(res.getBody().success).to.be.true;

    // Salva o ID da conta e o ID do usuário para usar nas rotas de Account e User
    let data = res.getBody().data;
    bru.setEnvVar("adminAccountId", data.accountResponse.id);
    bru.setEnvVar("userId", data.accountResponse.user.id);
  });
}

================================================================================
FILE: identity/admin/Deactivate Admin.bru
================================================================================
meta {
  name: Deactivate Admin
  type: http
  seq: 6
}

patch {
  url: {{baseUrl}}/identity/admins/{{adminAccountId}}/deactivate
  body: none
  auth: none
}

headers {
  Accept-Language: pt-BR
}

tests {
  test("Should deactivate the admin account successfully", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().success).to.be.true;
  });
}

================================================================================
FILE: identity/admin/Delete Admin.bru
================================================================================
meta {
  name: Delete Admin
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/identity/admins/{{adminAccountId}}
  body: none
  auth: none
}

headers {
  Accept-Language: pt-BR
}

tests {
  test("Should delete the admin successfully", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().success).to.be.true;
  });
}

================================================================================
FILE: identity/admin/Get Admin By ID.bru
================================================================================
meta {
  name: Get Admin By ID
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/identity/admins/{{adminAccountId}}
  body: none
  auth: none
}

headers {
  Accept-Language: pt-BR
}

tests {
  test("Should retrieve the admin by ID", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().success).to.be.true;
  });
}

================================================================================
FILE: identity/admin/Get Admin By Various Filters.bru
================================================================================
meta {
  name: Get Admin By Various Filters
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/identity/admins/by-email/admin.master@pug.com
  body: none
  auth: none
}

headers {
  Accept-Language: pt-BR
}

docs {
  Você pode testar os filtros trocando a URL do request:
  - {{baseUrl}}/identity/admins/by-email/admin.master@pug.com
  - {{baseUrl}}/identity/admins/by-cpf/73184295079
}

tests {
  test("Should retrieve the admin by filter", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().success).to.be.true;
  });
}

================================================================================
FILE: identity/admin/List or Search Admins.bru
================================================================================
meta {
  name: List or Search Admins
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/identity/admins
  body: none
  auth: none
}

params:query {
  ~q: Administrador
}

headers {
  Accept-Language: pt-BR
}

tests {
  test("Should list admins", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().success).to.be.true;
    expect(Array.isArray(res.getBody().data)).to.be.true;
  });
}


================================================================================
FILE: identity/admin/Update Admin.bru
================================================================================


================================================================================
FILE: identity/user/Get User By CPF.bru
================================================================================
meta {
  name: Get User By CPF
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/identity/users/by-cpf/00000000000
  body: none
  auth: none
}

headers {
  Accept-Language: pt-BR
}

tests {
  test("Should retrieve user by CPF", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().success).to.be.true;
  });
}


================================================================================
FILE: identity/user/Get User By ID.bru
================================================================================
meta {
  name: Get User By ID
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/identity/users/019cf2fd-837d-799b-a963-e1b299da07df
  body: none
  auth: none
}

headers {
  Accept-Language: pt-BR
}

tests {
  test("Should retrieve the user", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().success).to.be.true;
  });
}


================================================================================
FILE: identity/user/List or Search Users.bru
================================================================================
meta {
  name: List or Search Users
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/identity/users
  body: none
  auth: none
}

params:query {
  ~q: Administrador
}

headers {
  Accept-Language: pt-BR
}

tests {
  test("Should list users", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().success).to.be.true;
  });
}


================================================================================
FILE: partner/entity/Create Entity.bru
================================================================================
meta {
  name: Create Entity
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/partner/entities
  body: json
  auth: none
}

headers {
  Accept-Language: pt-BR
  Content-Type: application/json
}

body:json {
  {
    "cnpjString": "12345678000195",
    "name": "Tech Corp Parceira",
    "cityId": "{{cityId}}",
    "address": "Rua da Tecnologia, 100"
  }
}

tests {
  test("Should create an entity successfully", function() {
    expect(res.getStatus()).to.equal(201);
    expect(res.getBody().success).to.be.true;
    // Salva o entityId para os testes de Staff
    bru.setEnvVar("entityId", res.getBody().data.id);
  });
}

================================================================================
FILE: partner/entity/List Entities.bru
================================================================================
meta {
  name: List Entities
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/partner/entities
  body: none
  auth: none
}

headers {
  Accept-Language: pt-BR
}

tests {
  test("Should list entities", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().success).to.be.true;
  });
}

================================================================================
FILE: partner/staff/Create Staff.bru
================================================================================
meta {
  name: Create Staff
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/partners/staff
  body: json
  auth: none
}

headers {
  Accept-Language: pt-BR
  Content-Type: application/json
}

body:json {
  {
    "cpfString": "12345678909",
    "name": "Gestor da Parceria",
    "emailString": "gestor@techcorp.com",
    "password": "SecurePassword123!",
    "entityId": "{{entityId}}"
  }
}

tests {
  test("Should create staff successfully", function() {
    expect(res.getStatus()).to.equal(201);
    expect(res.getBody().success).to.be.true;
    bru.setEnvVar("staffAccountId", res.getBody().data.account.id);
  });
}
