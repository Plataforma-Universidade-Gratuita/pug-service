meta {
  name: Get User By CPF
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/identity/users/by-cpf/00000000000
  body: none
  auth: bearer
}

headers {
  Accept-Language: pt-BR
}

tests {
  test("Should retrieve user by CPF", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().success).to.be.true;
  });
}

auth:bearer {
  token: {{jwtToken}}
}
