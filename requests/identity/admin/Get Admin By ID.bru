meta {
  name: Get Admin By ID
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/identity/admins/{{adminAccountId}}
  body: none
  auth: none
}

headers {
  Accept-Language: pt-BR
}

tests {
  test("Should retrieve the admin by ID", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().success).to.be.true;
  });
}