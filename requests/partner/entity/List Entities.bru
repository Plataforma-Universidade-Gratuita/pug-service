meta {
  name: List Entities
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/partner/entities
  body: none
  auth: none
}

headers {
  Accept-Language: pt-BR
}

tests {
  test("Should list entities", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().success).to.be.true;
  });
}