meta {
  name: Delete Student
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/academic/students/{{studentAccountId}}
  body: none
  auth: bearer
}

headers {
  Accept-Language: pt-BR
}

tests {
  test("Should delete the student successfully", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().success).to.be.true;
  });
}
auth:bearer {
  token: {{jwtToken}}
}
